{
  "name": "contextual-retriever-framework-v2",
  "version": "0.1.0",
  "description": "Retrieval-augmented generation pipeline with vector search and context synthesis using Xiaomi MiMo for grounded answers.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "start": "ts-node src/index.ts"
  },
  "keywords": ['rag', 'embeddings', 'vector-search', 'mimo'],
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "ts-node": "^10.9.2",
    "vitest": "^1.6.0",
    "eslint": "^9.5.0"
  }
}